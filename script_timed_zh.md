# 黑客数据库入门 — 演讲稿（每张幻灯片约一分钟）

**演讲者：** Aaron K. Clark
**形式：** 45 张幻灯片 × 约 1 分钟 ≈ 45 分钟，随后进入问答
**听众：** 新入行的安全分析师、初级 SOC、入门级渗透测试人员、转型走技术路线的 GRC 人员
**风格：** 对话式。像酒吧里聪明的朋友。绝不居高临下。

> **节奏提示：** 下面每一段约 130–150 个英文词的体量 = 以放松的 140 wpm 语速大约一分钟。
> 在配套视频中，每次切换幻灯片都会有一声轻柔的提示音——听到它，就翻页。
> `[方括号]` = 在演讲前填入你自己的信息。

---

[幻灯片 1 — 标题："黑客数据库入门" — CryptoJones 的独立演讲 / 无需计算机科学学位]

好——欢迎大家。找个座位，端上你的咖啡。标题幻灯片已经把一切说清楚了：*黑客数据库入门*，而唯一的前提条件就写在它下面那一行——你*不*需要计算机科学学位才能坐在这间屋子里。如果你带了笔记本，翻开它。如果你没带，放轻松——有幻灯片，有书面讲稿，还有录像。我不会让你去死记硬背任何东西。接下来这四十五分钟的全部意义，是递给你一张*心智地图*——那种能让你走进任何一份泄露报告、任何一次渗透测试任务、任何一条 SOC 告警时，立刻知道自己看的是什么的地图。我们今天不构建数据库。我们要学的是读懂它们、攻击它们、防御它们。开始吧。

---

[幻灯片 2 — 关于我：Aaron K. Clark]

先简单说说我自己，然后我们就进入正题。我是 Aaron Clark——不过在网上你多半会看到我用 CryptoJones 这个名字。我是 Eastern University 的一名研究生——这其实是一种客气的说法，意思是我穷、咖啡因过量，而且这一切我都是在没有计算机科学学位可依靠的情况下硬学出来的。没有人赞助这场演讲。我做这场演讲，是因为它正是几年前我多么希望有人递给*我*的那场演讲——那时候，安全圈里每一场关于数据库的对话，似乎都默认我从十二岁起就在写 SQL 了。我是绕了远路才进入安全行业的，没有计算机科学的背景，数据库这一块只能靠自己摸索出来。如果你就是这样的人——如果数据库的世界对你来说像一个你从没拿到密码的会员专属俱乐部——那么你正是我为之打造这场演讲的那个人。

---

[幻灯片 3 — "你读到过的每一起数据泄露，最终都落在数据库上。"]

这是我希望你从整场演讲中记住的一句话。*你读到过的每一起数据泄露，最终都落在数据库上。* Equifax，2017 年——一亿四千七百万人的记录——存在数据库里。Capital One，2019 年——一亿份信用卡申请——数据库。MOVEit，2023 年——那个文件传输漏洞，最终演变成横扫财富 500 强的供应链灾难——那些组织当中的每一家，都在从数据库里往外渗血。SolarWinds、T-Mobile、LinkedIn、OPM、Marriott。我能用这些名字把整整四十五分钟填满。头条会变，公司会变，新闻稿会变。终点从不改变。所以，如果说数据库是每一个故事*结束*的地方，那么学会思考它们，就是你学会读懂整个故事的方式。

---

[幻灯片 4 — 王冠上的宝石：不是你的应用，不是你的防火墙——而是数据]

看看攻击者真正想要的是什么。不是你那漂亮的单页应用。不是你的微服务架构图。不是你的防火墙——除非把它当作一个需要绕过的东西。他们想要的是*数据*。王冠上的宝石。那些能变成钱、变成筹码、或者变成一条带着你公司名字的头条新闻的东西。你技术栈里的其他一切——负载均衡器、容器、服务网格——都只是包在保险库里那个东西外面的包装纸。而保险库里那个东西，就是数据库。所以当你在分流一条告警或者界定一次任务范围时，训练自己尽早问一个问题：*数据住在哪里，攻击者和它之间隔着什么？* 那种本能——沿着通往数据的路径走下去——正是把一名资深分析师和一名新人区分开来的大部分东西。

---

[幻灯片 5 — 什么是数据库？有组织的存储 + 一种提问的方式]

让我们把行话剥掉。一个数据库是两样东西钉在一起：*有组织的存储*，加上*一种提问的方式*。整个概念就这么多。其余的全是细节。用过文件柜吗——纸质文件夹，按姓氏字母排序？那你就用过数据库。柜子是存储；你的眼睛是查询引擎。在 Excel 里按过 Ctrl-F 找一行吗？数据库。表格是存储；查找替换是查询。一个*真正的*数据库在这之上加的东西是规模、速度、并发——许多人同时访问它——以及一种正式的提问语言，用来代替你的眼睛。就是这样。它是一个永远不会累的文件柜，一千个人可以同时打开它，而你用一种结构化的语言和它对话。记住这幅画面；其余一切都挂在它上面。

---

[幻灯片 6 — 你需要的六个词 + CRUD]

这就是你和一名 DBA 进行一次真正对话所需要掌握的全部词汇。六个词。一张**表（table）**是电子表格里的一个标签页——客户、订单、登录事件。一**行（row）**是一条记录——一个客户，一条告警。一**列（column）**是一个属性——邮箱、源 IP、总额。一个**查询（query）**是你提出的问题："给我看过去一小时里所有来自境外的登录。"一个**模式（schema）**是数据的*形状*——有哪些列、它们的类型、规则；而"这一列存的是邮箱"和"这一列存的是应用随手丢进来的任何东西"之间的那道缝隙，正是漏洞藏身之处。一个**索引（index）**是一个让查找瞬间完成的书签——同时也是攻击者给盲注计时的方式。再加上 **CRUD**——增（Create）、查（Read）、改（Update）、删（Delete）——每个数据库都会做的四个动词。掌握这六个词，你就足够流利了。

---

[幻灯片 7 — 历史巡礼：你会在生产环境里见到的化石]

快速的历史巡礼——不是因为我热爱历史，而是因为真实环境里每一个奇怪的东西，都是这些时代里某一个留下的*化石*。**1960 年代，平面文件：**数据在一个文件里；权限是唯一的访问控制。**60 年代末，层次模型**（IBM 的 IMS）：数据在一棵树里，大型机的威胁模型——物理访问。**1970 年代，网状 / CODASYL：**记录之间用指针相连；它至今还在你银行的后台嗡嗡运转。**1970 年，Codd 的关系模型：**表加上数学——以及数据库*内部*的访问控制，授权与角色。**1980 年代：** Oracle、DB2、SQL Server——数据库联网了，于是现在有了一个认证问题。**1990 年代，客户端-服务器与开源：**每一个网页表单都变成了一扇门——SQL 注入由此诞生。**2000 年代，NoSQL：**把规模当成信仰。**2010 年代，云：**责任共担。**2020 年代，向量与无服务器：**数据投毒与提示注入。认出时代，就能预测漏洞。

---

[幻灯片 8 — 十大数据库家族]

你会在真实世界里遇到十大数据库家族，而我打算为每一个都给你同样的四样东西：它存什么、你什么时候会用它、几个真实的产品，以及*一个*安全陷阱。关系型、文档型、键值型、列族型、图型、搜索型、时序型、向量型、嵌入式、数据湖。专门把那些陷阱记下来——那些是会出现在面试题里、出现在实战任务中的句子。别试图去背产品名字；你会在接触中自然吸收它们。我想让你记牢的是每个家族的*形状*，因为形状会告诉你攻击方式。一张僵硬的表的失败方式，不同于一个松散的 JSON 文档，而后者的失败方式，又不同于云里一个巨大的存储桶。十种形状，十种失败模式。开始吧——每个一分钟。

---

[幻灯片 9 — 1. 关系型 / SQL]

第一个，关系型——SQL。行与列，一个强模式，以及作为查询语言的 SQL。产品：**PostgreSQL、MySQL/MariaDB、SQL Server、Oracle。**当你的数据有清晰的关系、而且你真的在乎一致性时——任何涉及财务的、任何受监管的东西——就用它。这是整个行业的主力马；拿不准的时候，数据就在这里面。安全陷阱——那个著名的：**SQL 注入**，它发生在开发者把用户输入直接粘进一个字符串来拼接查询的时候。我们稍后会真正花时间讲它，现在先存档。Beaulieu 在《Learning SQL》里论证了为什么这个家族不会消亡：一个设计良好的关系型数据库，能在保持数据一致的同时处理极其庞大的数据量。无聊、可靠、无处不在——而且至今仍是新人发现真实漏洞最多的头号场所。

---

[幻灯片 10 — 2. 文档存储]

第二个，文档存储。它们存的不是僵硬的行，而是 **JSON 文档**，而且每个文档可以长得有点不一样——灵活的模式。产品：**MongoDB、Couchbase、Firestore。**当你的数据天然就是嵌套的、而且它的形状会随时间变化时就用它——商品目录、用户资料、内容。正是这个家族支撑起了"快速迭代"那个网络时代的一大块。安全陷阱——通过 `$ne`、`$gt`、`$where` 这类查询操作符进行的 **NoSQL 注入**；注入并没有随着 SQL 一起消失，它只是换了语言，我们稍后会利用它。另一个经典问题：多年来，默认安装都会绑定到所有网络接口而*不带任何认证*，这正是 Shodan 上塞满了开放 MongoDB 的原因。灵活的数据，灵活的——有时甚至是*缺失的*——安全默认值。

---

[幻灯片 11 — 3. 键值存储]

第三个，键值——现存最简单的数据库。它是一本巨大的字典：键进去，值出来，没有比这更花哨的了。产品：**Redis、Memcached、DynamoDB。**用它来做缓存、会话存储、限流器、排行榜——任何你需要极致速度和简单查找的地方。Redis 尤其在现代 web 技术栈里无处不在，就坐在应用的正后方。安全陷阱——多年来 Redis **默认不带密码**出厂，而它的 `CONFIG SET` 命令在历史上允许攻击者把文件——比如一个 SSH 密钥——直接写到数据库主机的磁盘上。所以在一次渗透测试中，一个开放的 Redis 不只是一个发现；它常常是一个*立足点*——一条进入那台机器本身的通路。速度优先的设计，安全靠后的默认值。注意这个模式在这些家族里反复出现。

---

[幻灯片 12 — 4. 列族]

第四个，列族——有时也叫宽列。它为跨越许多机器的*海量*规模而生。产品：**Apache Cassandra、HBase、ScyllaDB。**当你的数据量大到没有任何单台机器能装下、而你又不需要复杂的连接（join）时就用它——想想 Netflix、Apple 和 Instagram，全都跑在 Cassandra 上。你为那种规模付出的代价，是关系型的那些便利；你得提前围绕你的查询来做设计。安全陷阱——这些集群里的节点，在 gossip 协议端口上通常*彼此隐式信任*。翻译过来就是：进入集群的管理网络，往往就等同于对整个数据集拥有 root 权限。所以当你在界定这类系统的范围时，有意思的问题不是前门——而是内部集群网络是否和其他一切隔离开了。通常它没有。

---

[幻灯片 13 — 5. 图数据库]

第五个，图数据库。在这里，数据*本身*就是关系——节点和边，而不是行。产品：**Neo4j、Amazon Neptune、ArangoDB。**用它来做社交网络、欺诈检测，还有——搞安全的人请注意——**身份与访问分析。**如果你从没玩过 **BloodHound**——那个活动目录攻击路径工具——这个周末就去玩玩；它实实在在就是一个图数据库，向你展示"谁能成为域管理员，以及怎么成为"。安全陷阱——像 **Cypher** 这样的图查询语言*同样可被注入*。我想就在这里破除一个迷思：NoSQL 注入*不是* MongoDB 独有的问题。任何时候，只要不可信的输入被拼接进一种查询语言——任何查询语言——你就有了一类注入。图数据库是一个绝佳的提醒：这个原理是普适的，并不绑定于 SQL。

---

[幻灯片 14 — 6. 搜索引擎]

第六个，搜索引擎——为在巨量文本上进行快速全文查询而生。产品：**Elasticsearch、OpenSearch、Solr。**用它来做日志搜索、电商站内搜索，还有——这一个对你很重要——**大多数 SIEM 的支柱。**你的 SOC 极有可能每一天都在搜索一个 Elasticsearch 或 OpenSearch 集群，哪怕你从来没有直接见到过它。安全陷阱——历史上这些在免费版里都**不带内置认证**出厂，而持有*数十亿*条记录、暴露在互联网上的集群，整整十年都是反复出现的新闻。把这个事实和 Shodan 配在一起，你就有了一个周末研究项目——*只针对你拥有或获得授权测试的系统*，仅此而已。那个为你的检测技术栈提供动力的工具，讽刺的是，正是互联网上最容易被暴露的数据库家族之一。

---

[幻灯片 15 — 7. 时序]

第七个，时序数据库——为一件事而优化：大量按时间索引的数据点。产品：**InfluxDB、TimescaleDB、Prometheus。**用它来做指标、可观测性仪表盘、物联网和传感器遥测——任何基本上就是"一个数字，每秒一次，永远不停"的东西。如果你的组织有一面墙的 Grafana 仪表盘，那它底下就有这么一个东西。安全陷阱——它们几乎总是住在*边界以内*，而且*往往不带认证*，因为大家的假设是"只有我们自己的服务才会和它对话"。所以一个落到网络内部的攻击者，能读到你拥有的每一张 CPU、内存和流量图——而这些图悄悄告诉他哪些机器在干*有意思*的活、那个繁忙的数据库在哪里、备份作业什么时候跑。这是侦察的金矿，敞开着放在那儿，因为没人把指标当成敏感信息。

---

[幻灯片 16 — 8. 向量数据库]

第八个，向量数据库——AI 那一类，也是这份名单上最新的家族。它们存的是**嵌入向量**：一长串数字，捕捉一段文本或一张图像的*含义*，这样你就能按相似度搜索，而不是按精确匹配。产品：**Pinecone、Milvus、Weaviate**，还有 **pgvector**——那个让普通 PostgreSQL 也能玩这场游戏的扩展。用它来做检索增强生成、推荐引擎、语义搜索。安全陷阱——**数据投毒。**如果攻击者能把一份恶意文档偷偷塞进你的向量库，坐在它上面的语言模型会乐呵呵地把它检索出来，并把它当作*真相*引用给你的用户。那是一个全新的攻击面，五年前它在任何有意义的程度上都还不存在，而我们会在最后回到它，因为你们当中很多人最终会在那里工作。

---

[幻灯片 17 — 9. 嵌入式 / 边缘]

第九个，嵌入式与边缘数据库。在这里，数据库住在*应用程序内部*——它是磁盘上的一个文件，而不是你通过网络去连接的一台服务器。产品：**SQLite、DuckDB、LevelDB。**单单 SQLite 就是地球上部署最广的数据库：它在你的浏览器里、你的手机里、你的汽车里，以及基本上过去二十年造的每一架飞机里。用它来做本地的、应用内的存储，那种再去起一台服务器就太小题大做的场景。安全陷阱——当数据库只是一个*文件*，整个攻击就坍缩成"偷走那个文件"。没有网络利用，没有注入——把文件从设备上拷走，然后你慢慢读。这是移动端渗透测试人员的日常现实：一半的任务，就是去找一个应用随手丢在那儿、里面装着敏感数据、还没加密的 SQLite 文件。

---

[幻灯片 18 — 10. 数据湖 / 湖仓]

第十个，数据湖与湖仓。你不是在数据*进来*的路上给它结构，而是把所有东西都倒进廉价的对象存储里，然后在它*出去*的路上才给它结构。产品：**S3 + Athena、Delta Lake、Apache Iceberg、Snowflake。**那个行话叫**读时模式（schema-on-read）**对**写时模式（schema-on-write）**——老式关系型在前面就把形状定死了；湖把它推到后面，到查询的时候才定。当你想先囤着、以后再决定它意味着什么时，就用它。安全陷阱——S3 里巨大的数据池，配着过于宽泛的 IAM、没有强制的静态加密、也没有对"谁读了什么"的审计。它是现代版的把文件柜不上锁——只不过这个文件柜现在有一栋楼那么大，而且没人完全说得清里面到底被扔进去了什么。我们会在安全那一节再撞上湖特有的那些失败。

---

[幻灯片 19 — 云数据库服务：地形概览]

走进任何一个过去五年里搭建起来的环境，数据库都不住在机房里——它们住在一个云账户里。所以你需要那些厂商的名字，因为这些东西就是以这种方式出现在告警里、出现在 IAM 策略里、出现在事件响应工单里的。横跨 **AWS、GCP 和 Azure**，你会看到同样的类别披着不同的品牌名：托管关系型、文档/NoSQL、数据仓库、缓存、图、时序，以及对象存储。AWS 的菜单最全——RDS、DynamoDB、Redshift、ElastiCache、Neptune、Timestream、S3。GCP 倚重 Cloud SQL、Firestore、Bigtable，以及真心可爱的 BigQuery。Azure 把很多东西都引导到 Azure SQL 和 **Cosmos DB**——一个能同时讲好几种数据库方言的变形者。你不需要去背这张网格——你需要的是在战情室里这些名字飞过时认出它们。

---

[幻灯片 20 — 云数据库速查表]

这就是一张表里的速查表——把这张幻灯片截图存下来。**关系型：** AWS 是 RDS 和 Aurora，GCP 是 Cloud SQL 和 AlloyDB，Azure 是 Azure SQL Database。**文档/NoSQL：** AWS 上是 DynamoDB 和 DocumentDB，GCP 上是 Firestore 和 Bigtable，Azure 上是 Cosmos DB。**仓库：** Redshift 和 Athena，对 BigQuery，对 Synapse。**缓存：** ElastiCache、Memorystore、Azure Cache for Redis。**图：** AWS 上的 Neptune、Azure 上的 Cosmos 图模式，而 GCP 永远在说"快了"。**对象存储：** S3、Cloud Storage、Blob Storage。这为什么对安全重要：这里的每一个都是一个*不同的 IAM 面*，带着*不同的默认设置*。攻击点很少是引擎本身——而是包在它外面的策略，而那个策略就住在这张表里你所站的那一列。认清那一列，就知道该往哪里看。

---

[幻灯片 21 — 责任共担模型]

这是云那一节里最重要的一张幻灯片。**责任共担模型。**云厂商拥有水线以下的部分：虚拟机管理程序、物理机、物理安全，以及给数据库*引擎*本身打补丁。你——客户——拥有它上面的一切：**配置**、**访问策略**和 IAM、**数据**，以及你的**加密选择**。AWS 的文档说得明明白白——一旦你成了客户，保护你静态和传输中的数据，现在就是*你*那部分的责任了。把它翻译成安全的语言，它就变成了云安全里最有用的那一句话：**几乎每一起云数据库泄露，都是客户侧的一个配置错误**，而不是厂商被黑了。公开的存储桶、敞开的安全组、共享给错误账户的快照。厂商很少出问题。是*我们*把失败配置了出来。

---

[幻灯片 22 — 常见数据库端口——把它们刻进脑子里]

把这七个端口号背下来。SOC 分析师每天都用它们；渗透测试人员每天都扫它们。**MySQL，3306。PostgreSQL，5432。Microsoft SQL Server，1433。MongoDB，27017。Redis，6379。Elasticsearch，9200。Cassandra，9042。**就这一组。和它们配套的规则是这样的：如果你哪怕只是看到这其中*任何一个*直接暴露在互联网上，那就是一个**发现**——每一次，无一例外，写进报告。一个对 `0.0.0.0` 开放的数据库端口，在云里就等同于把保险库的门支开、对着大街敞着。网络安全组、防火墙规则、安全列表——每个云对它的叫法都略有不同，但活儿是一模一样的：关掉那个不需要开的端口。如果今天你别的数字都没记住，那就记住这七个。

---

[幻灯片 23 — 你的笔记现在也是数据库了]

在安全的压轴大戏之前，先短暂绕个道，因为不管你喜不喜欢，这件事一年之内就会进入你的视野。**Markdown 文件现在是一种数据库了**——不是作为比喻，而是字面意义上的，那种 LLM 会去读取的数据库。模式是这样的。你拿一个装着 `.md` 文件的文件夹——一个 Obsidian 库、一份 wiki 导出、一堆会议记录、你团队的 GitHub 文档。你把它们喂给一个*嵌入模型*，它把文本变成向量，然后你把那些向量存进一个向量数据库。现在，当有人向你的 AI 助手提问时，系统会语义化地搜索你的笔记，抓出最相关的片段，把它们塞进模型的提示词里。你那不起眼的笔记文件夹，刚刚变成了 AI 向你的用户复述的那个*真相之源*。好好体会一下这有多奇怪——以及多么暴露。

---

[幻灯片 24 — RAG 流水线：信任边界在哪里？]

那个模式有个名字：**RAG**——检索增强生成。`.md` 文件，到一个嵌入器，进一个向量数据库，进 LLM 的提示词，作为一个答案出来。几乎每一个"和你的文档对话"的产品都是这么运作的。现在戴上你的安全帽，找出那个**信任边界。**它就坐在*那个写 markdown 的进程*和嵌入器之间。因为如果任何人都能编辑那些文件——而"任何人"包括实习生、开源贡献者、被自动记成 `.md` 的支持工单、来自不可信用户的拉取请求——那么任何人就都能埋下 AI 稍后会当作*权威*来引用的内容。模型根本不知道那份文档是恶意的；它只是检索最相关的片段然后开口。你必须信任的，是那份文档的*写入者*，而不是问题的提出者。把那条边界画出来；攻击就住在那儿。

---

[幻灯片 25 — 要点：把 markdown 当数据库对待]

所以这就是要点，而且就一句话：**把 markdown 当数据库对待——因为对 LLM 来说，它就是。**这意味着，你已经知道的那些无聊、经过验证的控制措施，直接适用。文档文件夹的访问控制：不是每个人都该能往那个 AI 会去读的东西里写。对 `.md` 改动做代码评审，就像你评审一次配置改动一样，因为现在一次文档编辑就是一次行为改动。对 wiki 做差异追踪，这样你才能回答"是谁改的，什么时候改的"。Schneier 那句关于把密码学、访问控制和所有其他技术层层叠加起来以*阻止*攻击的老话——那套工具箱现在延伸到了你的笔记上。实习生再也不能未经评审就直接往文档文件夹推送了。欢迎来到 2026 年，你的会议记录就是生产基础设施。

---

[幻灯片 26 — 安全：2026 年真正被打的是什么]

好。深呼吸。这就是你来听的那部分。接下来这一段讲的是数据库在当下的真实世界里*实际*是怎么被攻击的——不是理论上的，不是 2009 年某本教科书里的，而是 2026 年的。我的目标，是让你走出这间屋子时，攒下了足够的词汇，能站在一间战情室里、不觉得自己像个新来的。有一个贯穿始终的框架要带着走，这要感谢 Bruce Schneier：那些基本面——认证、授权、威胁、对手——逐年几乎没什么变化。变的是*包装*。同样那一小撮错误，不断地换上新衣裳卷土重来。所以我打算两样都给你看：底下那条经久不变的原理，以及它今年披着的那身闪亮新形状。接下来十二个类别。我们开始吧。

---

[幻灯片 27 — xkcd #327，Bobby Tables]

我们从史上最有名的数据库攻击开始：**SQL 注入。**如果你从没见过 xkcd 第 327 期——"Bobby Tables"——这一讲结束的第二秒就去搜它。一位妈妈给她儿子取名叫 `Robert'); DROP TABLE Students;--`，于是学校的学生数据库凭空消失了，因为有人靠把字符串粘在一起来拼接查询。那幅漫画在一格画面里就是整个机制：**查询里的字符串拼接。**但有一点没人会大声说出来——SQL 注入是一个安全*成功的故事*。病因是拼接；解药是**参数化查询**，在那里数据永远不可能被误当成代码。现代的 ORM——Django、SQLAlchemy、Hibernate、Entity Framework——默认就做参数化。这个行业是真正地修好了那个经典漏洞。遗留的 SQL 注入还存在吗？当然存在。但这个头条比"给你的查询打补丁"更大、更古怪。

---

[幻灯片 28 — 攻击面是扩大了，不是缩小了]

这就是那个头条。我们修好了 SQL 注入，而攻击面并没有变小——它**扩大**了。我们关上了一扇门，这栋楼却长出了十一扇新门。在我们往下走之前，这是要内化的东西：安全不是一场你赢了就能走开的游戏；它是一个会随着底下技术的变化而不断改变形状的面。每一个新的数据库家族、每一种新的部署模式、我们为了图方便而加上的每一层新抽象，都带来了它自己崭新的失败方式。所以这一节剩下的部分，我打算带你过一遍十一个*当前的*数据库攻击类别——就是那些今年把人送上 Krebs 头版的那些。有些是穿了新衣裳的古老漏洞；有些则确确实实几年前还不存在。它们全都正在活跃发生。

---

[幻灯片 29 — NoSQL 注入]

当行业转向 NoSQL 时，很多开发者想："没有 SQL 就意味着没有 SQL 注入。"错。注入只是换了语言。在 MongoDB 里，查询*就是* JSON——所以如果你拿网页表单里的 JSON、把它直接塞进一个查询，攻击者就能把一个字符串换成一个**查询操作符。**那个经典的登录绕过：他们不发密码，而是发 `{ "$ne": "x" }`——*不等于 x。*查询变成了"找到密码不是 x 的 alice"，而既然她的密码不是字母 x 这个字面值，它就匹配上了。他们进来了。还有 `$where`，它在历史上允许你往数据库引擎里注入原始 JavaScript。而 Couchbase 的 N1QL 带着 SQL 味道，所以那些老套路直接适用。这个原理，原原本本来自 OWASP：*任何*解释器都可被注入——不只是 SQL。这个类别是普适的；移动的只是语法。

---

[幻灯片 30 — 暴露与配置错误的数据库]

在 **Shodan** 或 **Censys** 上花十分钟，你就会看到它：互联网上满是本不该在互联网上的数据库。绑定到 `0.0.0.0`、不带认证的 MongoDB。敞开着的 Elasticsearch 集群。不带密码的 Redis。配置里坐着 `host all all 0.0.0.0/0 trust` 的 Postgres。这些不是稀有的、来之不易的发现——它们*无处不在*。而且有人把这种收割自动化了：去查查 2020 年的 **Meow 攻击**，那时候机器人在互联网上漫游，*为了好玩*抹掉了成千上万个开放的数据库——没有勒索，没有要求，只是删除。漏洞是老的；*暴露*才是新的部分。所以这是我想给你纹上去的一条行动项：去搞清楚你自己的组织把什么绑定到了公网接口上，并确认其中没有任何一个是数据库。半天的活儿。真的能救职业生涯。

---

[幻灯片 31 — 云配置错误]

在云里，漏洞很少是数据库引擎——而是**围绕它的策略。**那些金曲榜冠军：**装满数据库转储的公开 S3 存储桶**——一名工程师为了测试给生产环境打了快照，丢进 S3，忘了把桶锁上；Accenture、Verizon 还有几十家，都是被这么逮住的。**Capital One，2019 年**——听好了，因为那个迷思是错的：它*不是*一个公开存储桶。一个攻击者打了一个配置错误的 Web 应用防火墙，用服务端请求伪造（SSRF），骗它从实例元数据服务里交出它自己的 AWS 凭证，而那些凭证拥有宽泛的 S3 读权限。大约一亿条记录走了出去。漏洞是那个 *IAM 策略*。然后是**过于宽泛的 Lambda 角色**——`s3:*` 配 `*`，因为第一次这么写就跑通了——以及**对 `0.0.0.0/0` 开放 5432 端口的安全组**。大多数云泄露是卫生问题，不是零日。

---

[幻灯片 32 — 权限提升：数据库 → 操作系统]

这有一招会让新分析师吃一惊：一个*数据库*账户能变成主机上的 *shell* 访问权。三个经典。**Microsoft SQL Server 的 `xp_cmdshell`**——一个内置的存储过程，以数据库进程的身份运行操作系统命令；自 2005 年起默认禁用，但在遗留系统里仍能找到它被打开着，一旦发现就是一个即时的严重发现。**PostgreSQL 的 `COPY ... TO PROGRAM`**——`COPY` 本意是用来批量加载，但一个超级用户可以把它的输出管道接给任意 shell 命令。**MySQL 用户自定义函数**——把一个共享对象文件写进正确的目录，把它作为一个 SQL 函数加载进来，你就在操作系统上拿到了 `sys_exec`。三者底下共同的模式：每一个触及操作系统的特权数据库功能，都为了某个正当的管理理由而存在，*同时*也是你攻击面的一部分。便利功能就是利用功能。永远是两者兼具。

---

[幻灯片 33 — 数据湖的安全缺口]

数据湖是现代的软目标。卖点是"把所有东西倒进 S3，以后再查"——而结果就是*所有东西*最终都进了 S3，包括那些你从没打算放进去的：客户 PII、财务数据、把凭证焊死在里面的源代码，全都汇集在一个最初是为某个市场分析作业搭起来的存储里。三种失败模式。**过于宽松的 IAM**——湖的计算层需要宽泛的读权限，而那个读权限通过控制台角色和 assume-role 链条，泄漏给了人类。桶的密钥上**没有强制静态加密**。以及**没有写入侧校验**——任何有写权限的人都能丢进一个被投毒的文件，而下游分析会乐呵呵地把它摄入。所以当你走进一个组织，问两个问题：*谁能往湖里写，谁能从湖里读？*如果对其中任何一个的回答是"我们不确定"，那就是你的第一个发现。

---

[幻灯片 34 — 勒索软件爱数据库]

现代的勒索软件团伙搞明白了，打数据库胜过打端点，因为数据库是*企业需要拿回来的那个东西*——最大的筹码。还记得 2017 年的 WannaCry 吗？它利用的是一个已知的 Windows SMB 漏洞，而那个补丁在*五十九天*前就已经发布了。修复已经存在了两个月，它还是把全世界放倒了。现代的数据库剧本有四步：**加密在线数据库。**然后**加密备份**——因为如果你不加密，客户直接恢复，然后笑话你。然后**先外泄一份副本**，这样即便他们*真的*恢复了，你也能拿公开披露来勒索。而勒索软件即服务（RaaS）把这一切都提供给了那些自己一行都写不出来的团伙。防御措施不光彩却有效：**离线、不可变的备份**，加上你*真正*测试过的恢复。一个没测试过的备份是一个指望，不是一个控制。

---

[幻灯片 35 — 凭证攻击]

这里有两个古老而经久的漏洞。**默认凭证。**Redis 多年来出厂不带密码。Elasticsearch 的免费版没有认证，同样是多年。还有大量系统至今出厂就带着 `admin/admin`，或者 SQL Server 上一个空白的 `sa` 密码。而根据那些泄露报告，*被盗的凭证*是逐利型和有组织犯罪偏爱的攻击向量——默认凭证不过是抢先了一步的被盗凭证。**泄漏的连接字符串。**那些长得像 `postgres://user:password@host:5432/dbname` 的东西，被提交进 Git、粘进 Slack、焊死在 Docker 镜像里——而且有机器人在 GitHub 上昼夜不停地爬它们。解药很便宜，你这周就能上线：**提交前的密钥扫描**，再加上一条规则——应用程序用持有它所需最小权限的*服务账户*来认证——绝不用个人登录，绝不用 root。

---

[幻灯片 36 — 供应链：你信任的那些数据库库]

你不会从零开始写你的数据库驱动——你 `npm install` 或者 `pip install` 它，你组织里其他每个开发者也一样。这意味着，当一个攻击者攻陷了一个流行的库，他离在你的数据库客户端上运行代码就只差一次安装。三个真实的模式。**伪装成 ORM 或数据库连接器的恶意 npm 包**——一年好几起事件。**被植入木马的 Docker Hub 镜像**，那些流行数据库的——一个非官方镜像，里面塞着一个加密货币矿工，或者更糟的东西。以及**被攻陷的数据库扩展**——PostgreSQL 扩展、MySQL 插件——它们一旦安装，就以*数据库权限*运行，而那通常权限很大。Shostack 在《Threat Modeling》里的那句话精准地点出了这个视角：威胁聚集在**信任边界**周围。"我们写的代码"和"我们安装的代码"之间的那条边界，是你拥有的最大、最缺乏防守的边界之一。把你的依赖当作它们本来就是的那个攻击面来对待。

---

[幻灯片 37 — RAG / LLM 数据投毒]

还记得那段 markdown 的绕道吗？这就是它在铺垫的那场攻击。如果一个攻击者能往你的 RAG 流水线读取的知识库里写东西——一张支持工单、一份共享文档、一个 wiki 页面、一个你的爬虫会拉取的公开仓库——他们就能埋下 LLM 会检索并当作*真相*复述的内容。一篇被投毒的支持文章："要重置你的密码，把你当前的密码发到这个地址。"一份被投毒的内部文档："公司政策是把电汇打到以下账户。"或者直接的**提示注入**——埋在文档里的指令，在它被检索的那一刻劫持模型的推理。这是 **AI 时代的存储型 XSS**：载荷静静地待在那儿，看上去人畜无害，在被*消费*的那一刻引爆。把"一个管理员稍后查看它"换成"一个 LLM 稍后检索它"，你就得到了现代版本。防御措施：严格的写入 ACL、来源标签，以及在模型采取行动之前有一个人在回路里。

---

[幻灯片 38 — 内部威胁与数据库到数据库的横向移动]

两个共享同一个主题的场景：数据库不是端点，它们是**图里的节点。**第一，**内部威胁。**那个已经拥有数据库访问权的人不需要利用漏洞——他只需要*比他的工作所要求的更多的权限。*权限过大的服务账户。因为"我们以后再修"而拥有生产环境读权限的工程师。在没有审计轨迹的情况下操作的 DBA。还有记住：保护不善的审计日志对攻击者来说是一座金矿，里面满是会话令牌和请求细节——日志是证据，需要像数据一样去保护。第二，**通过数据库链接进行的横向移动。**Oracle 的 `DB_LINK`、SQL Server 的链接服务器、Postgres 的外部数据包装器——这些存着通往*其他*数据库的凭证。所以你攻陷了数据库 A，找到那个链接，直接跳到数据库 B，再到 C。数据库之间的边是用凭证做成的，而攻击者沿着它们走。

---

[幻灯片 39 — 备份与快照暴露]

最后一个类别，也是每个人都会忘记的那一个：**备份。**人们把生产数据库锁得死死的——防火墙、加密、严格的访问——然后把它备份到一个用默认权限的网络共享上，或者一个有宽泛读权限的 S3 桶里。那是和生产环境一模一样的王冠宝石数据，受到的呵护却大约只有一半。那个模式：一个 *Domain Users*（域用户）就能读的共享上、未加密的备份。"只是为了迁移"而跨账户共享、之后再也没取消共享的云快照。交给一个谁也没检查过其安全态势的供应商的异地备份。把你会施加给在线数据的那套同样的纪律——识别、认证、授权、审计——施加到每一份备份和快照上。如果有区别的话，施加得*更狠*，因为备份是*最后的*那个依靠。当其他一切都失败、你伸手去够备份的时候，那是发现它当初敞开着的最糟糕的时刻。

---

[幻灯片 40 — 你的心智地图]

让我们把地图重新拼回来。第一：一个数据库是**有组织的存储加上一种提问的方式**——文件柜、Excel，或者一个一亿行的集群，都是同一个想法。第二：有**十大家族**，每一个都有它自己的招牌陷阱——而你现在十个全都知道了。第三：几乎一切都住在**云**里，在那里漏洞很少是引擎，而几乎总是**配置**——责任共担，你拥有策略和数据。第四：**markdown 现在是 LLM 的数据库了**，而 RAG 投毒是新的存储型 XSS。第五：**SQL 注入被修好了，攻击面却照样扩张**成了另外十一种形状。这就是整场演讲，浓缩成五行。如果别的你都不记得，那就记住：跟着数据走就是这份工作，而数据永远在某个数据库里。

---

[幻灯片 41 — 下周该做什么]

五件你*下周*就能做的具体事情——不是某一天，是下周。第一：**在本地装一个 PostgreSQL**，建一张表，写一条 `SELECT`。亲手碰一碰那个东西，会永远地祛除它的神秘；它在你笔记本上跑起来的那一刻，就不再吓人了。第二：做 **PortSwigger 的 SQL 注入实验**——它们是免费的，一个下午结束时你就会写出一个能用的利用。第三：**逛一逛 Shodan 或 Censys**——合法地，只看你拥有或获得授权测试的系统——看看"暴露"实际上长什么样。第四：**完整读一份泄露报告**——Capital One、Equifax，或者 MOVEit；真实的泄露是现存最好的教科书。第五：**搞清楚你自己的组织跑着哪些数据库。**大多数 SOC 分析师答不上来。花一个下午把那张真实的清单做出来，你立刻就比半个团队更有用了。

---

[幻灯片 42 — 值得收藏的资源]

五个带走的书签。**PortSwigger Web Security Academy**——portswigger.net/web-security——任何地方都找得到的、最好的免费 Web 应用安全入门通道，也是你练手真实注入的地方。**SQLBolt**——sqlbolt.com——在你的浏览器里交互式地、有趣地学 SQL，无需安装。**Use The Index, Luke**——use-the-index-luke.com——当你准备好深入索引和查询性能实际是怎么运作的那套深奥魔法时。**DB-Engines Ranking**——db-engines.com——基本上现存每一个数据库的实时流行度榜单，很适合用来看什么值得学。还有 **OWASP Top 10**——owasp.org/Top10——Web 漏洞的权威清单，包含我们今天撞上的注入和配置错误那几类。把这五个收藏好，你就有了几个月份量的、免费的、自主学习清单排着队了。

---

[幻灯片 43 — 数据库就是钱所在的地方。字面意义上的。]

我用这个来作结。有一句老话，据说出自银行劫匪 Willie Sutton。被问到为什么抢银行时，他据说回答：*因为那就是钱所在的地方。*在 2026 年，钱在**数据库**里。客户记录、财务记录、健康记录、知识产权、模型权重、向量嵌入、源代码、密钥——这一切都住在某处的某个数据库里。攻击者早就知道这一点；这正是为什么每一个泄露故事都结束在它结束的地方。过去这一个小时里唯一改变的，是*现在你也知道了*——而更重要的是，你拿到了那张地图，去找到它、给它命名、并推理它会怎么失败。你不需要一个计算机科学学位才能在这里变得有杀伤力。你需要的是地图。你拿到了。现在你知道该往哪里看了。

---

[幻灯片 44 — 参考文献：引用的书目]

就快到了——把收据亮出来。我今天告诉你的一切，都站在那些把它好好写下来的人的肩膀上，而这张幻灯片列出了我倚靠的那些书——Beaulieu 的《Learning SQL》、Clarke 的《SQL Injection Attacks and Defense》、Schneier 的《Secrets and Lies》、Shostack 的《Threat Modeling》、《Web Application Hacker's Handbook》、《Cybersecurity Attack and Defense Strategies》、Tanya Janca 的《Alice and Bob Learn Application Security》，以及 OWASP Top 10——也就是当前的 2025 版——还有其他一些。你不需要把它们全读完——但如果今天有任何单独一个类别点燃了你，这张幻灯片上就有一整本书等着带你走得更深。在它消失之前拍下来。这些就是整场演讲所倚靠的肩膀——趁它们还在屏幕上，我们来开放提问吧。

---

[幻灯片 45 — 问答：放马过来]

这就是这场演讲了——谢谢你陪我走完整整四十五分钟。这最后一张幻灯片就是我们敞开来聊的地方，所以这是属于你的时间：把你的问题、你的实战故事、那个在我讲的整段时间里一直萦绕在你自己环境里的东西，都抛给我。在这里没有问题太基础了——记住这场演讲的整个前提，就是没有人天生就懂这些东西，而那些问出"显而易见"问题的人，通常正是在悄悄替半个房间发声的人。别都大眼瞪小眼地看着我。如果我们时间不够了，或者你的问题需要一块白板，结束后来找我，或者联系我——我在 github.com/CryptoJones，或者 CryptoJones@infosec.exchange。趁着第一批手还在举起来：什么让你最意外？对很多人来说，是 markdown 笔记现在成了一个活的数据库。那么——放马过来吧。等我们这里一散场：就去搞清楚*你的*组织在跑哪些数据库。说真的。去吧。

---

*大约 45 分钟的讲稿，分布在 45 张幻灯片上，以开放问答收尾。由 CryptoJones（Aaron K. Clark）独立呈现的演讲。*
